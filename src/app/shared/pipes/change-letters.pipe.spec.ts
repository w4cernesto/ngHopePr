import { ChangeLettersPipe } from './change-letters.pipe';

describe('ChangeLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
